{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores user account information and profile data.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "fullName": { "type": "string" },
        "cpf": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "address": { "type": "string" },
        "documents": {
          "type": "object",
          "properties": {
            "frontUrl": { "type": "string" },
            "backUrl": { "type": "string" }
          }
        },
        "balance": { "type": "number" },
        "monthlyRevenue": { "type": "number" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "fullName", "email", "role"]
    },
    "AppConfig": {
      "title": "App Configuration",
      "description": "Global settings for the application.",
      "type": "object",
      "properties": {
        "facialVerificationEnabled": { "type": "boolean" },
        "banners": {
          "type": "array",
          "items": { "type": "string" }
        },
        "creditBannerUrl": { "type": "string" }
      }
    },
    "Payment": {
      "title": "Payment",
      "description": "Records of payments generated and their status.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "amount": { "type": "number" },
        "status": { "type": "string", "enum": ["pending", "paid", "expired", "refunded"] },
        "method": { "type": "string" },
        "externalId": { "type": "string", "description": "ID from the payment provider" },
        "description": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "amount", "status", "method", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Individual user profile documents."
    },
    "/config/app": {
      "schema": "AppConfig",
      "description": "Global application configuration."
    },
    "/payments/{paymentId}": {
      "schema": "Payment",
      "description": "Payment records."
    }
  }
}
